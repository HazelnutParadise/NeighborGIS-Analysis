```
your_project/
│
├── frontend/               # 前端專案資料夾
│
├── backend/
│   ├── main.py            # FastAPI 入口
│   ├── router/            # 存放 API 和前端頁面的路由
│   │   ├── __init__.py
│   │   ├── router.py      # 定義所有路由
│   │   ├── static.py      # 定義靜態檔案的路由（例如 /static/）
│   │   ├── page.py       # 定義靜態頁面的路由（例如 /index.html）
│   │   └── api.py   # 定義各種 API endpoints（例如 /geocode, /map 等）
│   ├── core/              # 核心設定（例如設定 CORS、中介軟體）
│   │   ├── __init__.py
│   │   └── config.py      # 各種設定參數
│   ├── services/          # 核心邏輯（原本 first_clean.py 和 geocoding.py 的功能）
│   │   ├── __init__.py
│   │   ├── geocoding.py
│   │   └── map_service.py
│   ├── structs/            # 資料結構（例如地理編碼請求與回傳格式）
│   │   ├── __init__.py
│   │   └── address_point.py
│   └── utils/             # 小工具（例如繪圖輔助函式）
│       ├── __init__.py
│       └── plot_helpers.py
│
├── Input/                 # 你的原始輸入資料夾（zoning shapefile 等）
├── output/                # 輸出的地圖、GeoJSON 等
├── requirements.txt       # 套件需求
└── README.md
```